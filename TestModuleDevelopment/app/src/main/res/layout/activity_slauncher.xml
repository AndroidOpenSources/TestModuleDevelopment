<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <android.support.v7.widget.Toolbar
        android:id="@+id/tb_common_toolbar_shop"
        android:background="?attr/colorPrimary"
        android:layout_height="@dimen/measure_common_tbar"
        android:fitsSystemWindows="true"
        android:layout_width="match_parent" >
        <!-- 该处为了兼容以上版本-->
        <!-- 中间控制层-->
        <FrameLayout
            android:id="@+id/frl_topbar"
            android:layout_width="wrap_content"
            android:layout_gravity="center"
            android:layout_height="wrap_content">
            <!--<android.support.design.widget.TabLayout-->
                <!--android:id="@+id/tab_common_shop"-->
                <!--app:tabMode="scrollable"-->
                <!--android:visibility="visible"-->
                <!--android:background="@android:color/transparent"-->
                <!--android:layout_width="wrap_content"-->
                <!--android:layout_height="@dimen/measure_common_tbar">-->
            <!--</android.support.design.widget.TabLayout>-->
        </FrameLayout>
        <!-- 内层操作，居右-->
        <TextView
            android:id="@+id/popMenu"
            android:layout_marginRight="20dip"
            android:layout_width="wrap_content"
            android:text=""
            android:layout_gravity="center_vertical|right"
            android:drawableLeft="@android:drawable/arrow_up_float"
            android:layout_height="wrap_content" />
    </android.support.v7.widget.Toolbar>
    <FrameLayout
        android:id="@+id/frl_launcher_sh"
        android:layout_below="@+id/tb_common_toolbar_shop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></FrameLayout>
</RelativeLayout>

